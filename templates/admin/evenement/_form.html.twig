        {{ form_start(form) }}
        <div class="row"> 
            <div class="col-md-3"> {{ form_row(form.type) }} </div>
            <div class="col-md-6"> {{ form_row(form.titre) }} </div>
            <div class="col-md-3"> {{ form_row(form.imageFile) }} </div>
            <div class="col-md-8"> {{ form_row(form.date) }} </div>
            
        </div>
        {{ form_widget(form) }}
            <button class="btn btn-primary">{{ button|default('Enregistrer')}} </button>
        {{ form_end(form) }}
        </div>