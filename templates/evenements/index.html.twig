{% extends 'base.html.twig' %}

{% block title %} {{ title }} {% endblock %}

{% block body %}

<div class="row container">
    <main class="example-wrapper page-evenement col-md-10">
        <h2> {{ titre }} </h2>
        <hr>
        <div class="container mt-4">
            <div class="row">
                {% for evenement in evenements %}
                <div class="col-4">
                    {% include 'evenement/_evenement.html.twig' %}
                </div>
                {% else %}
                    <li> Pas (encore !) d'évènements enregistrés</li>
                {% endfor %}
            </div>
        </div>
    </main>
    <aside class="col-md-2 p-0 m-0">
        {% include '/evenements/menuevenements.html.twig' %}  
    </aside>
</div>
{% endblock %}
