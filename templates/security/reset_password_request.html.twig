{% extends 'base.html.twig' %}

{% block title %}Réinitialisation de mot de passe{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script src="https://kit.fontawesome.com/c85d7d7f2c.js" crossorigin="anonymous"> </script>
{% endblock %}
{% block body %}
    
    <div class="alert alert-secondary mt-4 text-center"> Vous avez oublié votre mot de passe, ce n'est pas grave nous allons vous aider. 
            </br> Entrez simplement l'adresse mail liée à votre compte et nous vous enverrons un lien de réinitialisation.
        </div>
    <div class="example-wrapper page-login mt-4">
        {% include 'flashes.html.twig' %}
    
        <section class="mt-4">
            {{ form_start(form, 
                {'attr': {
                'class':'col-lg-12'
                }}
            )}}
            
            {{ form_row (form.username)}}
                   
            <div class="mt-2">
                {{form_row (form.email)}}
            </div>
            <button type="submit" class="btn btn-primary"> Envoyer mail de réinitialisation </button>
            {{ form_end(form)}}
        </section>
    </div>
    {% endblock %}